version: "3"
services:
  cloudflared:
    container_name: cloudflared
    image: sanoj2020/cloudflared:latest
#    image: sanoj2020/cloudflared:develop
    restart: always
    network_mode: host
    environment:
      - "token=your-cloudflare-tunel-token"
